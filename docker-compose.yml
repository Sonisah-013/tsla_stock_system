version: '3.8'

services:
  predictor-app:
    build: .
    container_name: tsla_predictor_container
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - ENV=production
    restart: always